/**
 * @packageDocumentation
 * @module api.functional.api.audit.action
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import type { Primitive, Resolved } from "typia";
import typia from "typia";

import type { AuditLogResponseDto } from "../../../../../modules/audit/dto";

/**
 * @controller AuditController.getActionLogs
 * @path GET /api/audit/action/:action
 * @accessor api.functional.api.audit.action.getActionLogs
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getActionLogs(
  connection: IConnection,
  action: string,
  limit?: number,
  skip?: number,
): Promise<getActionLogs.Output> {
  return true === connection.simulate
    ? getActionLogs.simulate(connection, action, limit, skip)
    : PlainFetcher.fetch(connection, {
        ...getActionLogs.METADATA,
        template: getActionLogs.METADATA.path,
        path: getActionLogs.path(action, limit, skip),
      });
}
export namespace getActionLogs {
  export type Output = Primitive<Array<AuditLogResponseDto>>;

  export const METADATA = {
    method: "GET",
    path: "/api/audit/action/:action",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (action: string, limit?: number, skip?: number) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries({
      limit: limit,
      skip: skip,
    } as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = `/api/audit/action/${encodeURIComponent(action?.toString() ?? "null")}`;
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
  export const random = (): Resolved<Primitive<Array<AuditLogResponseDto>>> =>
    typia.random<Primitive<Array<AuditLogResponseDto>>>();
  export const simulate = (
    connection: IConnection,
    action: string,
    limit?: number,
    skip?: number,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(action, limit, skip),
      contentType: "application/json",
    });
    assert.param("action")(() => typia.assert(action));
    assert.query(() => typia.assert(limit));
    assert.query(() => typia.assert(skip));
    return random();
  };
}
